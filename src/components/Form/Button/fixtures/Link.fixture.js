import React from 'react'
import Button from '../'

export default {
  component: Button,
  props: {
    submit: false,
    link: true,
    color: 'white',
    children: <div> Button</div>,
    small: true,
    disabled: false
  }
}
