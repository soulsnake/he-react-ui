import React from 'react'
import Button from '../'

export default {
  component: Button,
  props: {
    submit: false,
    color: 'grey',
    children: <div> Button</div>,
    small: true,
    disabled: false
  }
}
