import React from 'react'
import Button from '../'

export default {
  component: Button,
  props: {
    submit: false,
    color: 'white',
    children: <div> Button</div>,
    small: true,
    disabled: false
  }
}
