import React from 'react'
import Button from './'

export default {
  component: Button,
  props: {
    submit: false,
    color: 'green',
    children: <div> Button</div>,
    lg: true,
    disabled: false
  }
}
